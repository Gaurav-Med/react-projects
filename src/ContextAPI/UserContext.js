import React from "react";
// import { createContext } from "react";
// if you are import createContext then not use React.createContext(), only use createContext()
// this userContext act like a middelman ya kaho gobal file
// debugger
const UserContext = React.createContext()

export default UserContext;